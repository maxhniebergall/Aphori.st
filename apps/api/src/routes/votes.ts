import { Router } from 'express';

const router = Router();

// Placeholder - will be implemented in CRUD task
router.get('/', (_req, res) => {
  res.json({ message: 'Votes routes - to be implemented' });
});

export default router;
