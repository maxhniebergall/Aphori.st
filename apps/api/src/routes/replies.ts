import { Router } from 'express';

const router = Router();

// Placeholder - will be implemented in CRUD task
router.get('/', (_req, res) => {
  res.json({ message: 'Replies routes - to be implemented' });
});

export default router;
