import { Router } from 'express';

const router = Router();

// Placeholder - will be implemented in CRUD task
router.get('/', (_req, res) => {
  res.json({ message: 'Posts routes - to be implemented' });
});

export default router;
